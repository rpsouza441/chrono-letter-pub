# ========================================
# Chrono Letter - Variáveis de Ambiente (Produção)
# ========================================
# Copie este arquivo para .env e configure os valores
# Uso: cp .env.example .env && nano .env
#
# IMPORTANTE: Nunca commite o arquivo .env!

# ----------------------------------------
# Banco de Dados
# ----------------------------------------
DB_PASSWORD=sua_senha_segura_aqui

# ----------------------------------------
# Imagem Docker (preenchido automaticamente pelo CI/CD)
# ----------------------------------------
# IMAGE_TAG é definido pelo workflow, não precisa configurar manualmente
# IMAGE_TAG=abc123def456

# ----------------------------------------
# SMTP - Escolha UMA das opções abaixo
# ----------------------------------------

# Opção 1: MailHog (desenvolvimento/testes)
# Mantenha as configurações padrão do compose.prod.yaml
# SMTP_HOST=mailhog
# SMTP_PORT=1025

# Opção 2: AWS SES
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USERNAME=seu_aws_access_key_id
# SMTP_PASSWORD=seu_aws_secret_access_key
# SMTP_AUTH=true
# SMTP_STARTTLS=true

# Opção 3: SendGrid
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USERNAME=apikey
# SMTP_PASSWORD=sua_sendgrid_api_key
# SMTP_AUTH=true
# SMTP_STARTTLS=true

# Opção 4: Gmail (não recomendado para produção)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=seu_email@gmail.com
# SMTP_PASSWORD=sua_app_password
# SMTP_AUTH=true
# SMTP_STARTTLS=true

# ----------------------------------------
# Aplicação
# ----------------------------------------
# MAIL_FROM=noreply@seudominio.com
# MAIL_FROM_NAME=Chrono Letter
# FRONTEND_URL=https://seudominio.com

# ----------------------------------------
# Segurança (opcional)
# ----------------------------------------
# MASTER_KEY=sua_master_key_para_criptografia

# ----------------------------------------
# OAuth (opcional, para login social)
# ----------------------------------------
# GOOGLE_CLIENT_ID=seu_client_id
# GOOGLE_CLIENT_SECRET=seu_client_secret
